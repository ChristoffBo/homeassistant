{
  "id": "libvirt_service",
  "name": "Libvirt Service",
  "description": "Monitors libvirt virtualization daemon.",
  "check_cmd": "if command -v virsh >/dev/null; then systemctl is-active libvirtd || echo inactive; else echo skip; fi",
  "expected_output": "active|skip",
  "fix_cmd": "systemctl restart libvirtd || true",
  "verify_cmd": "systemctl is-active libvirtd 2>/dev/null || echo verified",
  "retry_count": 1,
  "retry_delay": 60,
  "ttl_seconds": 3600
}
