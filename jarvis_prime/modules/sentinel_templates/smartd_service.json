{
  "id": "smartd_service",
  "name": "SMART Disk Daemon",
  "description": "Checks that smartd is active to monitor disk health.",
  "check_cmd": "if command -v smartctl >/dev/null; then systemctl is-active smartd || echo inactive; else echo skip; fi",
  "expected_output": "active|skip",
  "fix_cmd": "systemctl restart smartd || true",
  "verify_cmd": "systemctl is-active smartd 2>/dev/null || echo verified",
  "retry_count": 1,
  "retry_delay": 60,
  "ttl_seconds": 3600
}
