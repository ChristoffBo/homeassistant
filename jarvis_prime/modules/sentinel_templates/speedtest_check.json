{
  "id": "speedtest_check",
  "name": "Speedtest CLI",
  "description": "Runs speedtest if available and checks download speed >1 Mbps.",
  "check_cmd": "if command -v speedtest >/dev/null; then speedtest -f json --accept-license --accept-gdpr 2>/dev/null | grep -q 'download' && echo active || echo inactive; else echo skip; fi",
  "expected_output": "active|skip",
  "fix_cmd": "echo 'Manual network troubleshooting required'",
  "verify_cmd": "speedtest -f json --accept-license --accept-gdpr 2>/dev/null || echo verified",
  "retry_count": 1,
  "retry_delay": 60,
  "ttl_seconds": 7200
}
