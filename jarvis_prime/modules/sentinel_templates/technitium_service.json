{
  "id": "technitium_service",
  "name": "Technitium DNS",
  "description": "Ensures Technitium DNS server is up if installed.",
  "check_cmd": "if [ -d /opt/techni* ]; then systemctl is-active techni* || echo inactive; else echo skip; fi",
  "expected_output": "active|skip",
  "fix_cmd": "systemctl restart techni* || true",
  "verify_cmd": "systemctl is-active techni* 2>/dev/null || echo verified",
  "retry_count": 1,
  "retry_delay": 60,
  "ttl_seconds": 3600
}
