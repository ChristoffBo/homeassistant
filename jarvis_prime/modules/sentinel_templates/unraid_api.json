{
  "id": "unraid_api",
  "name": "Unraid API Check",
  "description": "Queries Unraid web API for server status.",
  "check_cmd": "curl -fsS http://localhost/update.htm 2>/dev/null | grep -q up && echo active || echo inactive",
  "expected_output": "active|skip",
  "fix_cmd": "echo 'Manual Unraid restart required'",
  "verify_cmd": "curl -fsS http://localhost/update.htm 2>/dev/null || echo verified",
  "retry_count": 1,
  "retry_delay": 60,
  "ttl_seconds": 3600
}
